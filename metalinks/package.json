{
  "name": "metalinks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ efenstakes/metalinks",
    "create-local": "graph create --node http://localhost:8020/ efenstakes/metalinks",
    "remove-local": "graph remove --node http://localhost:8020/ efenstakes/metalinks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 efenstakes/metalinks"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
